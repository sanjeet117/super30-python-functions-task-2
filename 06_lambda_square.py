# Create a lambda function for calculating the square of a number.
square = lambda x: x**2
print(square)

square(5)

'''A lambda function is a small, one-line anonymous function that takes an input (x),
computes the square (x ** 2), and automatically returns the result without needing a def or return keyword.'''
